package time2;

import java.io.IOException;
import java.time.Instant;
import java.time.LocalDateTime;
import java.time.ZoneId;
import java.time.format.DateTimeFormatter;
import java.util.Scanner;

public class EpochToDate {

	public static void main(String[] args) throws IOException {
			//System.out.println(LocalDateTime.now().format(DateTimeFormatter.ofPattern("hhmmss")));
		
		
		//		Scanner in = new Scanner(System.in);
//		System.out.println("Enter epoch time in milliseconds");
		
//		while(true) {
//			if(in.hasNextLine()) {
//				long timeInEpoch = Long.parseLong(in.nextLine());
//				LocalDateTime timeInLocalDateTime = LocalDateTime.ofInstant(Instant.ofEpochMilli(timeInEpoch), ZoneId.systemDefault());
//				System.out.println(timeInLocalDateTime.getYear()+" "+timeInLocalDateTime.getMonth()+" "+timeInLocalDateTime.getDayOfMonth()+" "+timeInLocalDateTime.getHour()+":"+timeInLocalDateTime.getMinute());
//			}
//		}
		
	}

}
